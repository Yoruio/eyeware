


from ast import Continue


def main():
    # fetch data from glasses (mat, x,y)

    # object tracking: list(obj(label, contour))

    # object processing (seen/unseen, add to existing objects)

    # play sound
    Continue